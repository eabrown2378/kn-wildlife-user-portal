# Baked into the bundle at build time. `npm run build` reads this file; `npm run dev` does
# not, so local development still talks to http://localhost:8080.
VITE_API_BASE=https://kn-wildlife.crc.nd.edu
